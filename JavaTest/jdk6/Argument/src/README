[인자 값 체크]
1. 명칭
	- 인자 : 실행 파일 뒤에 따라오는 명령어
	- 옵션 : 등록된 명령어
	
2. 확인사항
	- 인자 범위 : 인자 최대 10개 이하 사용
		- 인자 클래스 String[](확정)
		- 인자 I/O는 한번에 들어옴(확정)
			> 동적 stack 메모리에 쌓임
		- 순서 있게 입력 : mode [option...]
	- 옵션 개수 : 10 ~ 100?
		- 옵션은 임의로 가져올 수 있음
			> 동적 stack에 쌓임
			> help 메시지(있는 그대로 출력 또는 정렬)
	
3. 진행방향
	1) 모든 옵션을 로딩해서 체크
		일정한 시간 = 변동이 거의 없음  50초
		메모리 사용량 많음
	2) 인자를 가지고 모든 옵션을 체크
		1~100초
		메로리 사용량 적음

	
	
4. java -jar innorules-inst.jar -s 
** 메모리 영역
정적
	- code : 함수, 상수 저장
	- data : 전역/정적 변수 저장
동적
	- stack : 지역/매개 변수 저장
	- heap : 동적 할당 영역